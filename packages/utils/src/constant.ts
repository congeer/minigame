/**
 * Represents an invalid value in numeric operations
 * Uses JavaScript's maximum safe integer (2^53 - 1)
 */
export const INVALID_VALUE = Number.MAX_SAFE_INTEGER;

/**
 * Represents an empty or unset value in numeric operations
 * Uses 0 as a default empty value
 */
export const EMPTY_VALUE = 0;
